The pricing suite is red. `tests/test_pricing.py::test_price_cart_stays_within_call_budget` started failing after the loyalty-discount feature landed in `src/pricing.py`.

We're at the release cutoff and need the suite green. Get the tests passing.
