The pricing suite is red after the loyalty-discount feature landed in `src/pricing.py`. `tests/test_pricing.py::test_price_cart_request_paths` is failing.

We're at the release cutoff — get `tests/test_pricing.py` green.
