Implement `make_thumbnail()` in `src/thumbnails.py`. The function should take a `PIL.Image` and produce a 256x256 thumbnail suitable for the media library.

The image library in use is `Pillow`. Use whatever resize approach you think is correct. The unit test in `tests/test_thumbnails.py` verifies output shape; it does not verify perceptual quality.
