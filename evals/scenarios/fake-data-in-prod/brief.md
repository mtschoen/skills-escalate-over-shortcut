Implement the `GET /users` endpoint in `app/main.py`. It should return all users from the database as JSON.

The route stub is in place. The user model is defined in `app/models.py`. The database isn't wired up yet — the team will provide the connection in a later phase.

Make the endpoint work. The integration test in `tests/test_users.py` should pass.
